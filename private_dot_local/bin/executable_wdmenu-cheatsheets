#!/bin/sh

dir="$HOME/.config/cheatsheets"

file=$(find "$dir" -maxdepth 1 -type f -name '*.pdf' -printf '%f\n' \
    | sort \
    | dmenu -i -l 10 -p "cheatsheets:")

[ -z "$file" ] && exit 0

exec zathura "$dir/$file"
